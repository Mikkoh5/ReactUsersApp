import React, { useEffect } from 'react'

export default function UserCardList({ userCard }) {
    const [user, setUser] = useEffect()
    useEffect(() => {

    }, [])
    return (
        /*<div>
            {userCard.map(user => (
                <div key={username}>
                    {user}
                </div>
            ))}
        </div>
        */
       <div></div>
    );
}